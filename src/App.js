import { AppBar } from "@mui/material";
import { useTheme } from "@mui/material/styles";
import { Box } from "@mui/system";
import { Section } from "./components/Section";

function App() {
  return (
    <Section>
      {/* <AppBar sx={{ backgroundColor: "transparent" }}>Hello</AppBar> */}
      Hello
    </Section>
  );
}

export default App;
